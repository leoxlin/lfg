# Agent instructions

- `docs/USAGE.md` is the project spec; treat it as the source of truth for behavior and conventions.
- When changing functionality, update `docs/USAGE.md` to match the new behavior.
- When adding features, document them in `docs/USAGE.md` before considering the change complete.
- Keep `docs/USAGE.md` and `lfg.zsh` in sync; users rely on the README as the canonical guide.
