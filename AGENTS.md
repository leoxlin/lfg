# Agent instructions

- `SPEC.md` is the project spec; treat it as the source of truth for behavior and conventions.
- When changing functionality, update `SPEC.md` to match the new behavior.
- When adding features, document them in `SPEC.md` before considering the change complete.
- Keep `SPEC.md` and `lfg.zsh` in sync; users rely on the README as the canonical guide.
